# Documentation

The documentation currently available is historical and is grouped under `legacy/` by the firmware version it originally described. It is useful background, but it may not match firmware v0.10.4 or the present prototype hardware.

The root README and `BUILDING.md` are the current starting points. A new consolidated assembly guide, bill of materials, wiring diagram, and service manual should be created before any formal release.

