# Development

This folder contains work that is not part of the frozen v0.8 baseline.

- `Older_Firmware/` preserves earlier firmware archives.
- `Experimental_Hardware/` contains transformer changes and active/passive audio-interface concepts that were not adopted into v0.8.

Validated changes should be assembled into the next complete project release rather than modifying v0.8 in place.
